# 更新日志

## 🏷️ v1.0.0-beta.7

### ✨ 新增

- 🌍 界面多语言（简体中文 / English）：全部页面文案（登录、侧边栏、任务列表、各类对话框、归档、回收站、Agent 接入、提示词、设置）双语化；首次启动按浏览器 / 系统语言自动选择，手动切换后记忆；顶栏与登录页均提供切换入口，选项以各自语言名称展示；日期时间格式、归档「今天 / 昨天」标签、标题排序区域随语言切换；系统分组「无分组」按界面语言显示（数据库存储名不变，后端保护逻辑与导出导入不受影响）；启动开屏文案同样跟随语言
- 🌍 后端消息多语言：HTTP API 错误提示按请求头 `Accept-Language` 返回对应语言（界面切换语言后错误提示同步，第三方调用 API 亦可指定）；MCP 工具描述、参数描述与全部运行时消息按 Agent 客户端 initialize 的 `locale` 返回，可用环境变量 `TODO4AGENT_LANG`（`zh` / `en`）强制指定，均未提供时默认中文（与旧版行为一致）；MCP 工具名称为协议契约，不随语言变化；后端全部文案集中为 Fluent 语言包（`locales/*.ftl`），缺失翻译自动回落中文
- 🔄 设置页新增「重启本应用」卡片：一键在界面内完成应用重启，修改监听端口、局域网访问等「重启后生效」的设置无需再手动退出重开；重启前弹出确认对话框，触发后等待服务恢复自动刷新页面，登录态与会话不受影响；桌面模式整体重启并自动重建窗口，serve 命令行模式优雅停机（释放端口）后自动拉起新进程；接口为 `POST /api/app/restart`，需登录后调用
- 🔗 路径保留当前页面：清单组与各页面写入地址栏（`/group/{分组id}`、`/archive`、`/trash`、`/mcp`、`/prompt`、`/settings`），刷新或直接打开链接停留在对应页面，浏览器前进 / 后退同步切换视图；登录后按路径恢复视图，分组 id 失效时回退到第一个分组；前端路径与 `/api/*` 接口路径互不影响
- ⌨️ 提示词编辑器升级为 CodeMirror 6：Markdown 语法高亮（标题、粗体、行内代码、代码围栏），增量解析打字零延迟，原生光标 / 选区 / 撤销；深浅主题跟随系统（浅色默认 / oneDark），字体沿用 Monaspace Neon；编辑器按需动态加载不增大首屏体积，加载失败自动退回纯文本

### 🚀 改进

- 🎨 卡片样式调整为 Vuetify 默认（elevated）投影样式：登录卡片、「Agent 接入」页与设置页共 8 处 v-card 由描边（outlined）改为投影卡片；「Agent 接入」页的命令标签（v-chip）样式保留
- ✏️ 代码块统一更换 Monaspace Neon 字体：「Agent 接入」页配置示例（含高亮不可用时的纯文本回退）与「提示词」页编辑器共用同一字体栈；字体文件（woff2）随应用打包，离线可用
- ⬆️ 前端依赖升级：Vuetify 3 → 4、Vite 6 → 8、TypeScript 5 → 6、vue-tsc 2 → 3、@vitejs/plugin-vue 5 → 6，并同步 package.json 版本声明与实际安装一致（重建 lockfile 不再回退大版本）；后端新增 fluent-i18n 依赖（Rust MSRV 提升至 1.85.1）
- 📱 手机端适配（<600px，与侧边栏移动端断点一致）：顶栏语言与刷新按钮（登录页语言按钮同效）、任务页排序与新建任务按钮切换为纯图标按钮（Vuetify 原生图标模式，48px 正方形、图标居中，无障碍标签保留），顶栏标题仅显示 Logo、隐藏应用名与副标题，消除标题文字的截断省略号；≥600px 恢复完整显示
- 💡 说明文本统一改为 ⓘ 悬停提示：新增 InfoTip 组件（悬停 / 键盘聚焦显示详情，与标题固定间距），「Agent 接入」页 MCP 介绍、「提示词」页功能说明、设置页「重启本应用」与「数据」卡片说明、归档页提示语收入各自标题旁的 ⓘ，界面不再出现大段说明文本；单行字段级提示（如端口重启提示）保留原样
- 🗂️ 归档页任务条目重写为专用扁平卡片：无投影浅底色条目，状态图标（已完成对勾 + 标题划线）+ 标题 + 分组 + 时间单行排布，描述单行截断保持密集；移除时间线（圆点与竖线占宽），保留按归档日期（今天 / 昨天 / 完整日期）分组；手机端元信息自然换行到标题下方并收紧内边距
- 📦 仓库迁移至 [github.com/Todo4Agent/Todo4Agent](https://github.com/Todo4Agent/Todo4Agent)：设置页「关于」、命令行帮助文档链接、MCP `app_release` 返回的发布页地址、README 下载链接与发布流水线说明同步更新

### 🐛 修复

- 🔒 修复系统分组「无分组」可被锁定的问题：锁定清单功能上线时未排除系统分组，而「无分组」承载删除分组后任务的兜底迁移（回收站恢复、取消归档回落同），一旦锁定这些任务将无法被 Agent 编辑；现界面不再显示其锁定入口，API 锁定请求返回可读错误（数据库层同样兜底），旧版本已锁定的存量数据在启动时自动解锁
- 📐 修复顶栏 Logo 与按钮图标未垂直对齐：Logo 原按行内文本基线对齐（text-bottom），随行高下沉约 2px，与右侧按钮图标不在同一水平线；改为标题内容层 flex 垂直居中（Vuetify 4 的 `.v-toolbar-title__placeholder`），顶栏内 Logo、应用名与各按钮图标中心线一致
- 🔄 修复刷新按钮不更新提示词：提示词页此前仅在首次打开时加载，Agent 经 MCP `prompt_update` 修改后点击顶栏刷新无效果；现在在提示词页点击刷新会以服务端内容重载编辑器（覆盖本地未保存的编辑，与任务列表刷新语义一致）

## 🏷️ v1.0.0-beta.6

### ✨ 新增

- 🗄️ 任务归档：任务卡片菜单（⋮ / 右键）新增「归档」，归档后从清单移入侧边栏新「归档」页，按归档日期分组的时间线展示（今天 / 昨天 / 完整日期），支持取消归档回原清单与移入回收站；归档任务删除后进回收站，恢复后保持归档态；MCP 新工具 `task_archive` / `task_unarchive`（锁定清单拒绝），`task_list` 新增 `include_archived` 参数（默认不含归档）
- 📂 系统分组「无分组」与任务保护：每个用户默认拥有不可删除、不可改名的「无分组」；删除分组时组内任务（含已归档与回收站中的）整体移入「无分组」，不再随分组进回收站或丢失，彻底删除分组 / 清空回收站亦不影响这些任务；取消归档、从回收站恢复任务时若原分组已删除，同样回落到「无分组」；界面与 MCP 对「无分组」的删除 / 改名返回可读错误
- 🛣️ MCP 新工具 `db_path`：查询当前连接的数据库文件路径（含 `TODO4AGENT_DB` 环境变量覆盖后的实际路径），方便 Agent 定位本地 SQLite 文件
- 📝 分组描述：清单组支持类似任务描述的备注字段，创建/编辑分组对话框可填写，清单页标题下方展示；MCP `group_create` 创建时与 `group_rename` 可选参数均可写入（锁定清单同样拒绝），`group_list` 返回 `description`；JSON 导出/导入包含分组描述（旧版文件兼容）
- ♻️ 回收站分组卡片重写：与任务卡片统一为同一套样式（共享样式文件），展示分组描述与随组删除的任务数
- 📜 安装包许可协议：Windows 安装包（NSIS / MSI）安装时展示 AGPL-3.0 许可页，用户同意后方可继续安装；deb / rpm 包随包附带许可证文件（AGPL-3.0-only）
- 🎨 安装包应用品牌图标：setup.exe 与卸载程序的文件图标替换为软件 logo，替换 NSIS 默认安装器图标
- 🔁 桌面端单实例：重复启动桌面应用不再开出第二个实例，而是唤起并聚焦已有窗口（含驻留托盘时）；第二个进程在启动 HTTP 服务与创建窗口之前即退出，不会抢占顺延端口或闪现窗口；`serve` / `mcp` 命令行模式不受影响，仍可多实例并行
- 🍎 macOS 支持 Intel 芯片：Release 同时提供 Apple Silicon（aarch64）与 Intel（x86_64）两种架构的 dmg 安装包，旧款 Intel Mac 也可直接安装运行

### 🚀 改进

- 🖱️ 任务卡片支持双击直接编辑（勾选框与操作按钮区域除外），与 ⋮ 菜单 / 右键菜单的编辑入口等效
- ⌨️ 回车提交忽略输入法组合状态：拼音打英文时按回车（上屏字母原文）不再被误当作确认，新建任务、分组名称与登录页输入框均适用
- 🖼️ 更换软件图标：新图标覆盖桌面端（安装包、托盘、窗口）、WebUI favicon 与开屏/顶栏 logo（由 1024×1024 透明源图经 tauri icon 生成全套尺寸）

### 🐛 修复

- 🧩 修复归档菜单「取消归档」项图标不显示：原用的 `mdi-package-up-outline` 不在 @mdi/font 7.x 字体内，换为 `mdi-archive-arrow-up-outline`（与「归档」的向下箭头成对）

## 🏷️ v1.0.0-beta.5

### ✨ 新增

- 🔒 清单锁定：按清单（分组）锁定，锁定后 Agent 通过 MCP 对该清单的写操作（该组任务增删改、移入该组、改名/删除该组、导入含同名分组）被拒绝并返回可读提示，读取与其他清单操作不受影响，界面编辑不受影响；在侧边栏分组 ⋮ 菜单或右键菜单一键切换，`group_list` 返回 `locked` 状态
- 📝 新增「提示词」页面：AGENTS.md 式的 Agent 协作规范，默认为空、内容由用户自行填写，支持编辑、复制与清空；数据按用户隔离存储，Agent 可通过 MCP 新工具 `prompt_get` / `prompt_update` 读写同一份提示词（传空字符串即清空）；提示词随 JSON 导出/导入迁移（`prompt` 字段，旧版文件兼容）
- 💫 开屏动画：启动时展示 logo 呼吸动画与加载指示，应用首屏就绪后淡出；开屏与页面背景在 HTML 首帧即随系统深浅色着色，桌面端窗口按系统主题设置原生背景后再显示，修复深色模式以白屏启动的问题
- 🔑 MCP 新增工具 `user_password`：修改当前账号密码（原密码 + 新密码），成功后吊销该用户全部已登录会话，并提醒同步更新客户端配置中的 `TODO4AGENT_PASSWORD`
- 💡 命令行新增 `help` / `version` 参数：`todo4agent help` 输出运行模式、MCP 客户端配置示例、初始账号与数据库路径，方便 Agent 首次运行自助接入；未识别的选项参数按用法错误退出，不再误入桌面模式
- 🔌 启动参数支持 `--port <端口>` 指定 WebUI/API 监听端口（1024-65535，也支持 `--port=端口`），本次运行有效、优先于设置页保存的端口；适用于桌面与 serve 模式，如 `todo4agent serve --port 8080`
- 📂 设置页「数据」区显示数据库文件路径，并新增「打开数据库文件位置」按钮：在系统文件管理器中定位该文件（Windows 选中文件 / macOS 在 Finder 中显示 / Linux 打开所在目录）
- ⚠️ MCP 凭据缺失时的报错会提示运行 `todo4agent help` 查看接入说明

### 🚀 改进

- ⚡ 会话校验改为直接查询数据库：桌面 / serve / mcp 多进程共用数据库时，任一进程的会话签发与吊销（如 MCP 改密踢出登录）对其他进程立即生效
- 🔐 应用始终要求认证（原免登录的「本地模式」为不可达路径，已移除），MCP 凭据必填与 README 一致
- 🎨 回收站任务卡片与普通清单统一为同一套卡片样式（共享样式文件，后续修改两处同步生效），并补充显示任务描述与截止时间
- 🌈 「Agent 接入」页配置示例代码块改用 Shiki 语法高亮（GitHub 浅色/深色双主题），替代自写的纯文本代码块，深浅色自动跟随系统切换；高亮加载失败时回退纯文本
- ♻️ 从回收站恢复分组遇同名冲突时自动顺延为「原名 (2)」，不再报数据库错误

### 🐛 修复

- 🧩 分组名唯一性改为按用户生效且不含回收站中的分组：不同用户可创建同名分组；删除分组后可立即新建同名分组；旧数据库打开时自动迁移
- 📌 修复侧边栏分组右键菜单位置向下偏移的问题：菜单改为挂载到 body，避免抽屉（v-navigation-drawer）的 transform 改变 fixed 定位参照
- 🗑️ 彻底删除默认分组「快速清单」后，不再于每次启动时被重新插入
- 💬 MCP `task_create` 指定不存在的分组时返回「分组不存在」，而非「数据库错误」

### 🔒 安全

- 🛡️ 密码哈希由单轮加盐 SHA-256 升级为 PBKDF2-HMAC-SHA256（600,000 轮），旧格式在下次登录时自动升级，无需用户操作
- 🚪 修改密码后自动吊销该用户的其他登录会话（当前登录保留）

## 🏷️ v1.0.0-beta.4

### ✨ 新增

- 👤 设置新增"允许注册新账号"开关，关闭后登录页不再显示注册入口
- 🧰 MCP 新增工具：app_version（查询版本号）、app_release（查询发布页地址）、group_delete（删除分组）、task_import（导入任务清单 JSON）
- 📋 MCP 客户端配置示例支持一键复制，用户名自动填入当前登录账号

### 🚀 改进

- 🌐 WebUI 默认监听 0.0.0.0，局域网内其他设备可直接访问；设置中可切换为仅本机
- ⬆️ Rust 后端依赖全面升级（rusqlite 0.40、rand 0.10、sha2 0.11、tower-http 0.7）

### 🐛 修复

- 🩹 修复 MCP 配置复制按钮不显示的问题
- 🌙 修复深色模式下 MCP 配置代码块过亮的问题
- 🖼️ WebUI 图标改用 ICO 文件，避免因系统缺少字体导致图标渲染异常

## 🏷️ v1.0.0-beta.3

### ✨ 新增

- 🖥️ 添加系统托盘，关闭窗口后应用驻留后台运行
- 🖱️ 接管右键菜单，任务与分组支持右键快捷操作
- 🌙 适配系统深色模式

### 🚀 改进

- 📦 Windows 安装包（MSI/NSIS）改为中文界面，发布版不再弹出黑色终端
- 🖼️ WebUI 使用软件图标（favicon 与顶栏 logo）
- 🚫 移除设置页注册入口，注册统一走登录页
- 🔑 密码字段适配密码管理器自动填表
- 🔄 刷新按钮同步任务列表，MCP 外部修改即时可见

## 🏷️ v1.0.0-beta.2

### 🐛 修复

- 🔖 优化版本号，解决 msi 打包问题

## 🏷️ v1.0.0-beta.1

🎉 发布首个测试版本。
