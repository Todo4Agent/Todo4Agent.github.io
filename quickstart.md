# 快速开始

## 安装

从 [GitHub Releases](https://github.com/Todo4Agent/Todo4Agent/releases) 下载对应平台的安装包即可。服务端口与对外监听可在「设置 → 服务」中修改（默认 3000 端口 / 监听 0.0.0.0）。

<DownloadLinks />

## 启动

安装后启动桌面应用，后台会同时提供 WebUI 服务：

- **桌面窗口**：与 WebUI 功能一致，关闭窗口后应用驻留系统托盘
- **浏览器访问**：<http://127.0.0.1:3000>
- WebUI 默认监听 `0.0.0.0`，局域网内其他设备可直接访问；可在「设置 → 服务」关闭对外访问（仅本机）或修改端口

无界面环境可使用 `todo4agent serve` 运行 WebUI / HTTP API，详见[命令行](/shell)。

## 登录

系统自带初始账号：

- 用户名：`admin`
- 密码：`admin123`

登录后请尽快在「设置 → 用户」中修改密码；也可以直接注册自己的新账号（注册按钮默认开启，可在「设置 → 用户」关闭）。多用户的任务数据互相独立。

## 主要功能

- **任务管理**：分组、新建、编辑、完成、截止时间、排序
- **清单锁定**：按清单（分组）锁定，锁定的清单 Agent 无法通过 MCP 编辑（界面编辑不受影响），适合只允许自己维护的清单
- **回收站**：误删可恢复
- **任务归档**：归档后移入「归档」页，按归档日期分组展示
- **导入 / 导出 JSON**：含任务清单与提示词，方便备份与迁移
- **跨平台**：桌面应用（Windows / macOS / Linux）与浏览器 WebUI
- **提示词**：AGENTS.md 式的 Agent 协作规范，默认为空、由用户自行填写，界面可编辑 / 复制 / 清空，Agent 也可通过 MCP 读写（数据按用户隔离）

## 下一步

- 要让 Agent 操作任务清单，见 [Agent 接入（MCP）](/mcp)
- 了解命令行运行模式与参数，见[命令行](/shell)
- 了解数据存储位置与备份迁移，见[数据](/data)
